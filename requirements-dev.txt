# Development and CI-only dependencies.
# Not installed in the production pipeline container.

-r requirements.txt

pytest==8.2.2
pytest-cov==5.0.0
pytest-xdist==3.6.1          # parallel test execution in CI
ruff==0.4.10                 # lint + format
mypy==1.10.0
types-PyYAML==6.0.12.20240311
pandas-stubs==2.2.2.240603
